{
  "name": "thomasfors-site",
  "version": "1.0.0",
  "private": true,
  "description": "Simple self-hosted gallery site and admin for Thomas Fors.",
  "main": "server/app.js",
  "scripts": {
    "start": "node server/app.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
